<Window x:Class="CookieClickerWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cookie Clicker" Height="550" Width="800"
        WindowStartupLocation="CenterScreen"
        Loaded="MainWindow_Loaded"
        Closed="MainWindow_Closed">

    <Window.Resources>
        <Style x:Key="CookieButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="CookiesText" Text="0 sušenek" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,15">
            <TextBlock x:Name="PerClickText" Text="Za kliknutí: 1" FontSize="14" HorizontalAlignment="Center"/>
            <TextBlock x:Name="PerSecondText" Text="Produkce: 0 / s" FontSize="14" HorizontalAlignment="Center"/>
        </StackPanel>

        <Button Grid.Row="2" x:Name="CookieButton" Cursor="Hand" Click="CookieButton_Click" Style="{StaticResource CookieButtonStyle}">
            <Image Source="pack://application:,,,/cookie.jpg" Width="220" Height="220" Stretch="Uniform"/>
        </Button>

        <Border Grid.Row="3" BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,10,0,10">
            <StackPanel>
                <TextBlock Text="Statistiky" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock x:Name="TotalClicksText" Text="Kliknutí: 0"/>
                <TextBlock x:Name="TotalUpgradesText" Text="Zakoupená vylepšení: 0"/>
                <TextBlock x:Name="TotalEarnedText" Text="Celkem získáno: 0"/>
            </StackPanel>
        </Border>

        <Button Grid.Row="4" x:Name="ShopButton" Content="Otevřít obchod" FontSize="16" Padding="10,5" Click="ShopButton_Click"/>
    </Grid>
</Window>