<!-- ...existing code... -->
<!-- ...existing code... -->
</Button>
  </Image>
    </Image.Style>
      </Style>
        </Style.Triggers>
          </DataTrigger>
            </Setter>
              </Setter.Value>
                <DropShadowEffect Color="#66000000" BlurRadius="10" ShadowDepth="4" Opacity="0.7"/>
              <Setter.Value>
            <Setter Property="Effect">
            </Setter>
              </Setter.Value>
                <ScaleTransform ScaleX="1.08" ScaleY="1.08"/>
              <Setter.Value>
            <Setter Property="RenderTransform">
            <Setter Property="Source" Value="pack://application:,,,/Assets/cookie_hover.png"/>
          <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}}" Value="True">
        <Style.Triggers>
        <Setter Property="Effect" Value="{x:Null}"/>
        </Setter>
          </Setter.Value>
            <ScaleTransform ScaleX="1" ScaleY="1"/>
          <Setter.Value>
        <Setter Property="RenderTransform">
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
      <Style TargetType="Image">
    <Image.Style>
         Source="pack://application:,,,/Assets/cookie.png">
         Width="128" Height="128"
  <Image x:Name="CookieImage"
        ToolTip="Klikni na sušenku">
        Cursor="Hand"
        BorderThickness="0"
        Background="Transparent"
        Width="140" Height="140"
<Button x:Name="CookieButton"
        Width="140" Height="140"
        Background="Transparent"
        BorderThickness="0"
        Cursor="Hand"
        ToolTip="Klikni na sušenku">
  <Image x:Name="CookieImage"
         Width="128" Height="128"
         Source="pack://application:,,,/Assets/cookie.png">
    <Image.Style>
      <Style TargetType="Image">
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
          <Setter.Value>
            <ScaleTransform ScaleX="1" ScaleY="1"/>
          </Setter.Value>
        </Setter>
        <Setter Property="Effect" Value="{x:Null}"/>
        <Style.Triggers>
          <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}}" Value="True">
            <Setter Property="Source" Value="pack://application:,,,/Assets/cookie_hover.png"/>
            <Setter Property="RenderTransform">
              <Setter.Value>
                <ScaleTransform ScaleX="1.08" ScaleY="1.08"/>
              </Setter.Value>
            </Setter>
            <Setter Property="Effect">
              <Setter.Value>
                <DropShadowEffect Color="#66000000" BlurRadius="10" ShadowDepth="4" Opacity="0.7"/>
              </Setter.Value>
            </Setter>
          </DataTrigger>
        </Style.Triggers>
      </Style>
    </Image.Style>
  </Image>
</Button>
<!-- ...existing code... -->
